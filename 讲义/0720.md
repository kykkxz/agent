# 0720 第5天 工作目标

1. 查漏补缺：
   第一组上周遗留问题：函数中的参数类型不清楚。
   第二组上周遗留问题：项目中模块之间的调度关系，不清晰。导入自定义模块的操作还不熟练
   第四组上周遗留问题：类方法，类装饰器使用还不够熟练，不懂在工程项目中的什么场景下使用最佳

2. 异步三个子任务讲解

3. 异步项目任务讲解

4. 学生成绩管理系统，项目结构梳理联系

5. mysql安装与基本语法
   
   

## 模块一：查漏补缺

1.函数形参的5大类型，混合定义时，是必须按照固定顺序定义

位置形参 → 默认形参 → *args可变位置参数 → 命名关键字形参 → **kwargs可变关键字参数

def abc(a：int,b,c)->string:

    输入-->函数--->输出

![8cf5a2d2-1736-4fc3-b83e-cdd9563c333c](file:///C:/Users/qiuxingyu/OneDrive/Pictures/Typedown/8cf5a2d2-1736-4fc3-b83e-cdd9563c333c.png)

2.通过手写项目代码关系图，进行梳理（包括文件之间的关系和函数之间的关系）

3.类方法，类装饰器：使用场景：工具调用计数/缓存管理，日常自己开发用到的场景不是太多，开源项目或者ai生成的代码会有，需要看得懂即可



@Cacher("DEBUG")

def add_num(a,  b):



## 模块二：异步三个子任务

任务

🎯 任务一（基础关，约 15 分钟）：异步打招呼

要求：

1. 写 `async def greet(name, delay)`：`await asyncio.sleep(delay)` 后，打印 `f"Hello, {name}!"` 并返回 `name`。
2. 用 `asyncio.gather` 同时跑 3 个：`("Alice", 1)`、`("Bob", 2)`、`("Carol", 3)`。
3. 打印返回的结果列表，打印总耗时（应该约 3 秒）。

```python
import asyncio
import time

async def geert(name, delay):
    await asyncio.sleep(delay)
    print(f"Hello, {name}!")
    return name

async def main():
    start = time.time()
    results = await asyncio.gather(
        geert("Alice", 1),
        geert("Bob", 2),
        geert("Carol", 3)
    )
    end = time.time()
    print(f"总耗时: {end - start} seconds")
    print("结果:", results)

asyncio.run(main())

print("\n测试完成！预计总耗时: 3.0 秒")
```



🎯 任务二（进阶关，约 15 分钟）：三种方式大对比

要求：

1. 写一个"模拟推理"的活：等 1 秒，返回结果。
2. 分别用**串行**（一个一个来）、**多线程**、**异步**三种方式跑 5 个活。
3. 用 `datetime` 记录每种方式的总耗时，打印出来对比。

```python
import asyncio
import time
import threading
import datetime

def sync_inference(name):
    time.sleep(1)
    return f"{name}结果"

async def async_inference(name):
    await asyncio.sleep(1)
    return f"{name}结果"

# 串行
print("=============串行推理=============")
start = time.time()
for i in range(5):
    sync_inference(f"T{i}")
end = time.time()
print(f"串行总耗时: {end - start} seconds")

# 多线程
print("=============多线程推理=============")
start = time.time()
threads = [threading.Thread(target=sync_inference, args=(f"T{i}",)) for i in range(5)]
for thread in threads:
    thread.start()
for thread in threads:
    thread.join()
end = time.time()
print(f"多线程总耗时: {end - start} seconds")

#异步
print("=============异步推理=============")
async def main():
    start = time.time()
    results = await asyncio.gather(*[async_inference(f"T{i}") for i in range(5)])
    end = time.time()
    print(f"异步总耗时: {end - start} seconds")

asyncio.run(main())
```



🎯 任务三（挑战关，约 20 分钟）：异步模拟多用户请求

要求（复用第三天的 AI 模型骨架，改成异步）：

1. 定义 `AIModel` 基类，`async def predict(self, input_data)` 抛 `NotImplementedError`。
2. 子类 `TextModel`：`predict` 里 `await asyncio.sleep(1)`，返回 `f"文本结果:{input_data}"`。
3. 子类 `ImageModel`：`predict` 里 `await asyncio.sleep(2)`，返回 `f"图像结果:{input_data}"`。
4. 写 `async def user_request(user, model, input_data)`：记录开始/结束时间，`await model.predict(...)`，返回 `{user, model, cost, result}`。
5. 用 `gather` 同时跑 4 个用户请求（2 个文本、2 个图像），打印每个用户耗时和总耗时。

```python
import time
import threading
from datetime import datetime


class AIModel:
    def __init__(self, name, model_type):
        self.name = name
        self.model_type = model_type

    def predict(self, input_data):
        print(f"{self.name}收到输入：{input_data}")
        return "父类默认结果"


class TextModel(AIModel):
    def predict(self, input_data):
        print(f"文本模型{self.name}生成中...")
        time.sleep(1)
        return f"文本结果: {input_data}"


class ImageModel(AIModel):
    def predict(self, input_data):
        print(f"图像模型{self.name}识别中...")
        time.sleep(2)
        return f"图像结果: {input_data}"


records = []
lock = threading.Lock()


def user_request(user_name, model, input_data):
    start = datetime.now()
    result = model.predict(input_data)
    end = datetime.now()
    cost = (end - start).total_seconds()
    with lock:
        records.append({
            "user": user_name,
            "model": model.name,
            "cost": cost,
            "result": result
        })


text_model = TextModel("GPT小助手", "文本生成")
image_model = ImageModel("图像识别器", "图像识别")

total_start = datetime.now()
threads = [
    threading.Thread(target=user_request, args=("用户A", text_model, "写首诗")),
    threading.Thread(target=user_request, args=("用户B", text_model, "讲个笑话")),
    threading.Thread(target=user_request, args=("用户C", image_model, "cat.jpg")),
    threading.Thread(target=user_request, args=("用户D", image_model, "dog.jpg")),
]
for t in threads:
    t.start()
for t in threads:
    t.join()
total_end = datetime.now()

print("\n=== 请求明细 ===")
for r in records:
    print(f"{r['user']} -> {r['model']}，耗时{r['cost']:.2f}秒，结果：{r['result']}")

print(f"\n并发总耗时：{(total_end - total_start).total_seconds():.2f}秒")
# 预期约 2 秒（图像模型最慢，2秒，并发后由最慢的决定）
```



## 模块三：AI推理系统优化为异步版本

```python
"""
AI 推理任务指挥官 —— 多线程 & 异步版
"""
import time
import asyncio
import threading
from datetime import datetime


# ========== 模型层 ==========
class AIModel:
    def __init__(self, name, model_type):
        self.name = name
        self.model_type = model_type

    def predict(self, input_data):
        raise NotImplementedError("子类必须实现 predict")


class TextModel(AIModel):
    def predict(self, input_data):
        start = datetime.now()
        print(f"    [{self.name}] 生成文本：{input_data}")
        time.sleep(1)
        end = datetime.now()
        return {"output": f"《{input_data}》的生成文本",
                "inference_cost": (end - start).total_seconds()}


class ImageModel(AIModel):
    def predict(self, input_data):
        start = datetime.now()
        print(f"    [{self.name}] 识别图像：{input_data}")
        time.sleep(2)
        end = datetime.now()
        return {"output": f"{input_data} → 识别为：猫",
                "inference_cost": (end - start).total_seconds()}


class VoiceModel(AIModel):
    def predict(self, input_data):
        start = datetime.now()
        print(f"    [{self.name}] 识别语音：{input_data}")
        time.sleep(1.5)
        end = datetime.now()
        return {"output": f"{input_data} → 文字：你好世界",
                "inference_cost": (end - start).total_seconds()}


# ========== 异步模型层 ==========
class AsyncAIModel:
    def __init__(self, name, model_type):
        self.name = name
        self.model_type = model_type

    async def predict(self, input_data):
        raise NotImplementedError("子类必须实现 predict")


class AsyncTextModel(AsyncAIModel):
    async def predict(self, input_data):
        start = datetime.now()
        print(f"    [{self.name}] 生成文本：{input_data}")
        await asyncio.sleep(1)
        end = datetime.now()
        return {"output": f"《{input_data}》的生成文本",
                "inference_cost": (end - start).total_seconds()}


class AsyncImageModel(AsyncAIModel):
    async def predict(self, input_data):
        start = datetime.now()
        print(f"    [{self.name}] 识别图像：{input_data}")
        await asyncio.sleep(2)
        end = datetime.now()
        return {"output": f"{input_data} → 识别为：猫",
                "inference_cost": (end - start).total_seconds()}


class AsyncVoiceModel(AsyncAIModel):
    async def predict(self, input_data):
        start = datetime.now()
        print(f"    [{self.name}] 识别语音：{input_data}")
        await asyncio.sleep(1.5)
        end = datetime.now()
        return {"output": f"{input_data} → 文字：你好世界",
                "inference_cost": (end - start).total_seconds()}


# ========== 调度器 ==========
class Scheduler:
    def __init__(self):
        self.records = []
        # self.lock = threading.Lock()

    def _run_one(self, user, model, input_data):
        start = datetime.now()
        result = model.predict(input_data)
        end = datetime.now()
        # with self.lock:
        self.records.append({
            "user": user, "model": model.name, "type": model.model_type,
            "input": input_data, "output": result["output"],
            "total_cost": (end - start).total_seconds()
        })

    def run_serial(self, tasks):
        self.records = []
        for user, model, x in tasks:
            self._run_one(user, model, x)

    def run_threading(self, tasks):
        self.records = []
        threads = []
        for user, model, x in tasks:
            t = threading.Thread(target=self._run_one, args=(user, model, x))
            threads.append(t)
            t.start()
        for t in threads:
            t.join()

    async def _run_one_async(self, user, model, input_data):
        start = datetime.now()
        result = await model.predict(input_data)
        end = datetime.now()
        # async with self.lock:  # 异步单线程，append是原子操作，不需要锁
        self.records.append({
            "user": user, "model": model.name, "type": model.model_type,
            "input": input_data, "output": result["output"],
            "total_cost": (end - start).total_seconds()
        })

    async def run_async(self, tasks):
        self.records = []
        # self.lock = asyncio.Lock()
        coros = [self._run_one_async(user, model, x) for user, model, x in tasks]
        await asyncio.gather(*coros)  #*[T1,T2,T3]= T1,T2,T3

    def report(self):
        print("-" * 60)
        for r in self.records:
            print(f"  {r['user']} -> {r['model']}({r['type']}) "
                  f"输入:{r['input']} | 输出:{r['output']} | 耗时:{r['total_cost']:.2f}秒")

    @staticmethod
    def compare(perf):
        print("\n" + "=" * 50)
        print("【性能对比报告】")
        print("=" * 50)
        for label, t in perf.items():
            print(f"  {label:<8}: {t:.2f} 秒")
        vals = list(perf.values())
        if len(vals) >= 2:
            print(f"  加速比  : {max(vals) / min(vals):.2f} 倍")
        print(f"  报告时间: {datetime.now().strftime('%Y-%m-%d %H:%M:%S')}")


# ========== 主程序 ==========
def build_tasks():
    text_model = TextModel("GPT小助手", "文本生成")
    image_model = ImageModel("VisionPro", "图像识别")
    voice_model = VoiceModel("语音小达人", "语音识别")
    return [
        ("用户1", text_model,  "写一首秋天的诗"),
        ("用户2", text_model,  "讲个冷笑话"),
        ("用户3", image_model, "photo1.jpg"),
        ("用户4", image_model, "photo2.jpg"),
        ("用户5", voice_model, "audio1.wav"),
        ("用户6", voice_model, "audio2.wav"),
    ]


def build_async_tasks():
    text_model = AsyncTextModel("GPT小助手", "文本生成")
    image_model = AsyncImageModel("VisionPro", "图像识别")
    voice_model = AsyncVoiceModel("语音小达人", "语音识别")
    return [
        ("用户1", text_model,  "写一首秋天的诗"),
        ("用户2", text_model,  "讲个冷笑话"),
        ("用户3", image_model, "photo1.jpg"),
        ("用户4", image_model, "photo2.jpg"),
        ("用户5", voice_model, "audio1.wav"),
        ("用户6", voice_model, "audio2.wav"),
    ]


def main():
    tasks = build_tasks()
    async_tasks = build_async_tasks()
    scheduler = Scheduler()
    perf = {}

    print("=" * 50); print("【串行模式】"); print("=" * 50)
    s = datetime.now()
    scheduler.run_serial(tasks)
    perf["串行"] = (datetime.now() - s).total_seconds()
    scheduler.report()

    print("\n" + "=" * 50); print("【多线程模式】"); print("=" * 50)
    s = datetime.now()
    scheduler.run_threading(tasks)
    perf["多线程"] = (datetime.now() - s).total_seconds()
    scheduler.report()

    print("\n" + "=" * 50); print("【异步模式】"); print("=" * 50)
    s = datetime.now()
    asyncio.run(scheduler.run_async(async_tasks))
    perf["异步"] = (datetime.now() - s).total_seconds()
    scheduler.report()

    Scheduler.compare(perf)


if __name__ == "__main__":
    main()
```

注意：

1.异步不需要加锁

2.*[T1,T2,T3]= T1,T2,T3   *刚在列表前面等于分解列表，变成多个单元素输入



## 模块四：python基础回顾

### 一.复习与面向对象

1.基本语法快闪

f"{name}今年{age}岁"

2.易错点：

```python
def add_item(item, bag =[]):
    bag.append(item)
    return bag
print(add_item("apple"))
print(add_item("banana"))

print("=============")

def add_item(item, bag =None):
    if bag is None:
        bag = []
    bag.append(item)
    return bag

print(add_item("apple"))
print(add_item("banana"))
```

3.类与对象

1）魔术方法__call___,__str__

2）私有属性，加一个下划线

3）类方法和静态方法，抽象方法

4）继承

super()  不止能用在__init__方法里，任何重写的方法都是能用的，表示”先调用父类的版本，再补充自己的逻辑“___

```python
class Animal:
    def __init__(self, name):
        self.name = name

    def speak(self):
        return "某种声音"

    def info(self):
        return f"这是{self.name}"


class Dog(Animal):
    def __init__(self, name, breed):
        super().__init__(name)   # 先调用父类初始化
        self.breed = breed       # 再补充子类特有属性

    def speak(self):
        parent_result = super().speak()   # 先要父类的
        return parent_result + "，具体是汪汪"

    def info(self):
        parent_info = super().info()      # 先要父类的info
        return parent_info + f"，品种：{self.breed}"


dog = Dog("旺财", "中华田园犬")
print(dog.speak())   # 某种声音，具体是汪汪
print(dog.info())    # 这是旺财，品种：中华田园犬
```

5）多态：不同的子类多项，调用同一个方法名，会表现出不同的行为。基于重写的表现形式

```class
class Animal:
    def __init__(self, name):
        self.name = name
    def speak(self):
        pass


class Dog(Animal):
    def speak(self):
        return "汪汪"


class Cat(Animal):
    def speak(self):
        return "喵喵"


class Cow(Animal):
    def speak(self):
        return "哞"


def make_sound(animal):
    print(animal.speak())


make_sound(Dog("旺财"))
make_sound(Cat("咪咪"))
make_sound(Cow("大黄"))
```

6）has-a 组合  ：类与类之间的关联并不是只有继承关系，has-a的关系，编程上的一个思维方法

```python
class Engine:
    def start(self):
        return "发动机启动"

class Car:
    def __init__(self):
        self.engine = Engine()   # Car 组合了 Engine

    def start(self):
        return self.engine.start() + "，汽车出发"

print(Car().start())   # 发动机启动，汽车出发
```

is-a 继承，has-a组合，灵活搭配



### 模块五：mysql的安装

1. mysql安装与使用
   1.1 mysql属于轻量级的数据库，sqlite3（本地用）
   1.2 sqlalchemy python第三方orm操作库，不是数据库，虚拟操作数据库
   --必须依赖真实数据库（mysql）才能运行
   --屏蔽数据库底层差异，一套python代码可以适配mysql，sqlite等所有关系型数据库
   --不需要写原生的sql，通过面向对象来操作数据

2. 本地mysql的检测和安装

1）[MySQL :: Download MySQL Installer](https://dev.mysql.com/downloads/installer/)、

2）建议full模式

![97f9539d-aa13-4a19-8c2b-d9ef4bd2ea4b](file:///C:/Users/qiuxingyu/OneDrive/Pictures/Typedown/97f9539d-aa13-4a19-8c2b-d9ef4bd2ea4b.png)

3）网络配置：默认端口 **3306**

4）勾选：开机自动启动服务（无需手动开启，上课直接用）

5）一路next

6）配置环境变量：

安装完成后必须配置环境变量，实现任意目录使用mysql命令

1. 找到MySQL安装bin目录，默认路径：`C:\Program Files\MySQL\MySQL Server 8.0\bin`（自定义安装的找到对应bin路径）

2. 右键「此电脑」→ 属性 → 高级系统设置 → 环境变量

3. 在「系统变量」中找到 **Path**，双击打开

4. 新建变量，粘贴上面的bin完整路径，一路确定保存

5. 关闭所有CMD黑窗口，重新打开生效

7）登录数据库

```
#查看版本
mysql --version
#登录数据库 
mysql -u root -p
```

![f7535a9c-8c58-4e23-9889-b76584cf786e](file:///C:/Users/qiuxingyu/OneDrive/Pictures/Typedown/f7535a9c-8c58-4e23-9889-b76584cf786e.png)

mysql：很多个数据库，每个数据库有很多表，每个表的数据主键id，属性/列



## 模块六：mysql的基本使用

MySQL 课程必备基础SQL语句（适配你当前 student_db 库）

## 一、数据库操作

### 1. 创建数据库

    -- 创建，不存在才创建，utf8mb4支持中文、表情
    CREATE DATABASE IF NOT EXISTS student_db DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

### 2. 查看所有数据库

    SHOW DATABASES;

### 3. 切换使用数据库

    USE student_db;

### 4. 删除数据库（谨慎使用）

    DROP DATABASE IF EXISTS test_db;

* * *

## 二、数据表操作

### 1. 创建数据表（示例学生表）

    CREATE TABLE IF NOT EXISTS student (
        id INT PRIMARY KEY AUTO_INCREMENT COMMENT '主键自增ID',
        name VARCHAR(50) NOT NULL COMMENT '学生姓名',
        age TINYINT COMMENT '年龄',
        score DECIMAL(5,2) COMMENT '分数',
        create_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间'
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

### 2. 查看当前库所有表

    SHOW TABLES;

### 3. 查看表结构

    DESC student;

### 4. 删除数据表

    DROP TABLE IF EXISTS student;

* * *

## 三、增删改查（CRUD 核心，Python SQLAlchemy高频使用）

### 1. 新增数据 INSERT

    -- 插入完整字段
    INSERT INTO student(name, age, score) VALUES ('张三', 18, 92.5);
    
    -- 批量插入多条
    INSERT INTO student(name, age, score)
    VALUES 
    ('李四', 19, 88),
    ('王五', 18, 95.5);

### 2. 查询数据 SELECT

    -- 查询整张表所有数据
    SELECT * FROM student;
    
    -- 查询指定字段
    SELECT id,name,score FROM student;
    
    -- 条件查询（年龄大于18）
    SELECT * FROM student WHERE age > 18;
    
    -- 排序（分数降序）
    SELECT * FROM student ORDER BY score DESC;
    
    -- 分页，只取前2条
    SELECT * FROM student LIMIT 2;

### 3. 修改数据 UPDATE（必须加WHERE，否则全表修改）

    -- 修改张三的分数
    UPDATE student SET score=96 WHERE name='张三';

### 4. 删除数据 DELETE（必须加WHERE）

    -- 删除id=3的学生
    DELETE FROM student WHERE id=3;

* * *

## 四、用户/权限相关（你刚重置root用到）

    -- 刷新权限
    FLUSH PRIVILEGES;
    
    -- 修改root密码为兼容模式
    ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY '123456';

* * *

## 五、辅助常用语句

    -- 查看当前正在使用哪个库
    SELECT DATABASE();
    
    -- 退出mysql终端
    exit;


