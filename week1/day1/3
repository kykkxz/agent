• 假设：DeferredRenderer 已构造，构造阶段已经执行过一次 SetupRenderGraph()，但还没有 Compile()；第二帧没有 resize、效果开关变
  化、shader reload。

  初始化阶段

  DeferredRenderer::DeferredRenderer()
    -> CreateResources()
    -> CreateDescriptorLayouts()
    -> CreateFullscreenQuad()
    -> GenerateSSAOSamples()
    -> SetupRenderGraph()
         -> Clear old graph
         -> CreateTexture(GBuffer_*)
         -> CreateTexture(ShadowMap*)
         -> ImportTexture(SSAO_Noise / Water_* / LUT / fallback)
         -> AddPass(Shadow*)
         -> AddPass(GBuffer / GBuffer_Water / GBuffer_Smoke)
         -> AddPass(SSAO / SSAO_Blur / SSGI)
         -> AddPass(DeferredLighting)
         -> AddPass(PostProcess chain)
         -> AddPass(ForwardOverlay)
         -> AddPass(Gamma / FXAA / FinalOutput)

  第一帧

  BeginFrame(time, dt)
    -> 更新 time / deltaTime
    -> 记录 curr view-projection
    -> 重置 object uniform cursor

  SceneRenderer 提交场景
    -> SyncLightingToDeferred()
         -> ClearLights()
         -> AddLight(...)
    -> SubmitRenderablesToDeferred()
         -> Submit(renderable, Opaque)
         -> Submit(renderable, Water)
         -> Submit(renderable, Smoke)

  Render(cmd)
    -> ApplyPendingRenderGraphRebuild()
         -> 第一帧通常无 pending，直接返回

    -> UpdateFrameUniforms()
         -> 上传相机矩阵、VP、上一帧 VP、cascade 矩阵等

    -> UpdateLightUniforms()
         -> 上传 lights[]
         -> 更新 directional / spot / point shadow uniform
         -> 更新 SSAO / SSGI / Bloom 等 uniform
         -> 动态启用/禁用 shadow pass

    -> if RenderGraph 未 compiled:
         CompileRenderGraph(width, height)
           -> RenderGraph::Compile(width, height)
                -> Validate()
                -> InferDependencies()
                -> TopologicalSort()
                -> CreateResources()
                -> BuildFramebuffer(pass)
                -> m_IsCompiled = true
           -> InvalidateCachedDescriptors()
           -> CreatePostProcessPipelines(false)
           -> TickPipelineWarmup()

    -> TickPipelineWarmup()

    -> ExecuteRenderGraph(cmd)
         -> 创建 RenderGraphContext
         -> 绑定 ShadowMap* 到 LightDescriptorSet
         -> RenderGraph::Execute(cmd, context)

  第一帧的 RenderGraph::Execute() 内部按拓扑顺序跑 pass，大致是：

  ShadowCascade0/1/2
  ShadowSpot*
  ShadowPoint*_Face*

  GBuffer
  GBuffer_Water
  GBuffer_Smoke

  ParticleSimulation
  SmokeParticleMask
  SmokeParticlePosition

  SSAO
  SSAO_Blur
  SSGI
  DepthVisual

  DeferredLighting

  Refraction / Reflection / Foam / BaseCombine   // 条件性
  Sharpen / Posterize / Bloom / Fog / SceneCombine / DOF / Outline ... // 条件性

  ForwardOverlay
  GammaCorrection
  ChromaticAberration or FXAA
  FinalOutput

  每个 pass 的内部模板是：

  CalculatePassBarriers(prevPass, currentPass)
    -> ApplyBarriers()

  if pass has framebuffer:
    -> BeginRenderPass(framebuffer, clear/load values)

  SetViewportAndScissor()
  pass->Execute(cmd, context)
    -> BindPipeline()
    -> BindDescriptorSet()
    -> Draw fullscreen quad / draw geometry / dispatch compute

  if pass has framebuffer:
    -> EndRenderPass()

  最后：

  EndFrame()
    -> 清空 m_Renderables / m_WaterRenderables / m_SmokeRenderables
    -> m_PrevViewProjection = m_CurrViewProjection

  第二帧

  第二帧没有 graph rebuild 时，最大区别是：不再 SetupRenderGraph()，也不再 RenderGraph::Compile()，直接复用第一帧编译好的资源、
  framebuffer、pipeline cache。

  BeginFrame(time, dt)
    -> 更新时间
    -> 记录 curr view-projection
    -> 重置 object uniform cursor

  SceneRenderer 提交场景
    -> SyncLightingToDeferred()
    -> SubmitRenderablesToDeferred()

  Render(cmd)
    -> ApplyPendingRenderGraphRebuild()
         -> 无 pending，返回

    -> UpdateFrameUniforms()
         -> 上传本帧相机数据
         -> 使用上一帧留下的 m_PrevViewProjection

    -> UpdateLightUniforms()
         -> 上传本帧灯光/阴影/效果参数

    -> if RenderGraph 未 compiled:
         跳过，因为第一帧已经 compiled

    -> TickPipelineWarmup()
         -> 可能继续分预算预热剩余 pipeline

    -> ExecuteRenderGraph(cmd)
         -> 更新 RenderGraphContext
         -> 必要时更新 shadow map descriptor
         -> RenderGraph::Execute()
              -> 同样的 pass 顺序
              -> 同样逐 pass barrier / render pass / callback / end

  EndFrame()
    -> 清空本帧提交列表
    -> 保存 curr VP 给第三帧使用

  一句话对比：第一帧是“提交场景 + 更新 uniform + 编译 RenderGraph + 创建 framebuffer/pipeline + 执行”；第二帧是“提交场景 + 更新
  uniform + 复用已编译图直接执行”。
